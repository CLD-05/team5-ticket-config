apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: alb
spec:
  parametersRef:
    group: elbv2.k8s.aws
    kind: LoadBalancerConfiguration
    name: ticket-gateway-config
    namespace: ticketing