apiVersion: v1
kind: ServiceAccount
metadata:
  name: ticket-service
  namespace: ticketing
  labels:
    app: booking
    app.kubernetes.io/name: ticket-service
    app.kubernetes.io/part-of: team5-ticket

  # AWS API 호출을 Pod Identity/IRSA로 전환할 경우 이곳에 role annotation 추가
  # prod 앱/워커용 IAM Role ARN 확정 후 적용
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::194722398200:role/team5-prod-ticket-service
